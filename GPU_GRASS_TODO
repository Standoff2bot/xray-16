1. Convert existing CPU structs into GPU-friendly layouts (`DetailInstanceGPU`, frustum params) and lock down HLSL parity. **Status: complete (C++/HLSL parity in place).**
2. Build the offline/export pipeline for clipmap-aligned compressed masks, height clipmaps, and object metadata, then verify deterministic placement parity versus the legacy decompressor. **Status: cache writer/loader and disk persistence complete; parity validation pending.**
3. Implement the clipmap residency manager (Regions→Tiles→Cells) with sparse bindings, tile lifecycle events, and optional CPU-side persistence for interaction data. **Status: initial residency/placement queue hooked up; needs real streaming + incremental instance updates.**
4. Author the compute placement shaders to expand compressed slots, sample height, apply jitter, and populate persistent instance buffers using deterministic hashing. **Status: per-tile allocation + culling/vertex path live; focus now shifts to performance validation and incremental streaming polish.**
5. Extend compute culling with hierarchical cell rejection, hi-Z hooks, LOD tagging, and multi-draw indirect submission while wiring true GPU instancing into the render path (including the optional procedural blade geometry switch).
6. Implement the interaction texture pipeline (decay, impulse, optional blur/SDF), CPU entity ingestion, and shader sampling integration across all LODs.
7. Build the FBM wind system with compute-generated vector fields, per-instance seeding, multi-height sampling, temporal smoothing, gust domain animation, and far-LOD flow textures.
8. Finalize streaming distances, far-LOD impostors/billboards, and ensure interaction/wind coherence; profile performance and tune tile/texture formats.
9. Remove legacy systems (grass_benders, detail cache manager, wave lists, CPU batching paths) after parity validation and document the GPU architecture for tooling teams.
