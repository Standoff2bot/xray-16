#include "common.h"

struct v2p
{
    float2 tc0 : TEXCOORD0;
    float4 c0  : COLOR0;
};

float4 main(v2p I) : SV_Target
{
    // Test: output pure red to verify shader is working
    return float4(1.0, 0.0, 0.0, 1.0);
}
